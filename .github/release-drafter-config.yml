# .github/release-drafter-config.yml

name-template: 'Lunaris Codex $RESOLVED_VERSION'
tag-template: 'v$RESOLVED_VERSION'

categories:
  - title: '🚀 Changes & Updates'
    labels: [] # Para commits diretos, as labels de PR não se aplicam automaticamente

change-template: '- $TITLE (commit $SHA_SHORT) by @$AUTHOR'

template: |
  ## Changes in v$RESOLVED_VERSION

  $CHANGES

  **Full Changelog**: https://github.com/$OWNER/$REPOSITORY/compare/$PREVIOUS_TAG...v$RESOLVED_VERSION

  ---
  *This Changelog was auto-generated by [Release Drafter](https://github.com/release-drafter/release-drafter).*

exclude-labels:
  - 'skip-changelog'

version-resolver:
  default: patch
